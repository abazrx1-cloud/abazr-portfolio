# Logo visual check

- Desktop preview: the uploaded gold ABAZR mark is visible beside the `ABAZR` wordmark in the dark header without stretching the full wide logo.
- Mobile preview at 375x812: the mark remains visible beside the name, the header stays within the viewport, and the mobile menu trigger remains reachable.
- The image is RGBA PNG at 1672x941; the CSS uses a clipped frame to show the mark while keeping the provided logo asset as the source.
- No layout overflow was observed in the captured top viewport.
